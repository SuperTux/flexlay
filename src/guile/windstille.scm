;; Init file ;;

;;(use-modules (ice-9 syncase)
;;	     (oop goops))

;;(load "dragon.scm")
;;(load "helper.scm")
;;(load "for.scm")

;;(define-syntax unpack
;;  (syntax-rules ()
;;    ((_ (data (var ...)) body ...)
;;     (call-with-values (lambda () data)
;;       (lambda (var ...)
;;	 body ...))
;;     )))

;;(load "scmdog.scm")
;;(load "nebular.scm")
;;(load "bounce.scm")

(display "Windstille script loaded...\n")


;; EOF ;;
